// import mongoose from 'mongoose';

// const pdfSchema = new mongoose.Schema({
//     filename: String,
//     data: Buffer,
//     contentType: String,
//     recordId: { type: mongoose.Schema.Types.ObjectId, ref: 'Record' } // เชื่อมโยงกับ Record
// });

// const PDF = mongoose.model('PDF', pdfSchema);

// export default PDF;